# DS 余额实时监测插件开发计划

> 插件名称（暂定）：`dsh-balance-monitor`
> 目标：根据 API key 实时监测 DeepSeek API 账户剩余余额——**会话内可查可见 + 浏览器侧边栏常驻显示 + 手动刷新按钮**。
> 本文档 = 需求拆解 + 方案选型 + 分阶段实施计划。技术事实均已在本机 dsh 源码与官方文档中核实。

---

## 0. 决策记录（用户已拍板）

| 决策点 | 结论 |
|---|---|
| 展示形态 | **会话内（工具查询 + 每轮注入）+ Web UI 侧边栏常驻显示** |
| API key 来源 | **复用 dsh 现有凭证** `DEEPSEEK_API_KEY`（零配置） |
| 刷新策略 | **30s TTL 缓存 + 30s 后台轮询 + UI 手动刷新按钮** |
| 低余额告警 | **不做** |
| 会话花费估算 | **不做**（官方无用量接口，估算非精确，放弃） |

---

## 1. 第一性原理拆解

### 1.1 「余额」是什么？

DeepSeek 开放平台账户是**预付费**模式。账户余额的唯一可信来源是官方余额接口：

```
GET https://api.deepseek.com/user/balance
Authorization: Bearer <API_KEY>
```

官方响应结构（字符串金额，不是浮点数）：

```json
{
  "is_available": true,
  "balance_infos": [
    {
      "currency": "CNY",
      "total_balance": "110.00",
      "granted_balance": "10.00",
      "topped_up_balance": "100.00"
    }
  ]
}
```

- `total_balance` = 总余额（`granted_balance` 赠送 + `topped_up_balance` 充值）
- 币种可能同时返回 CNY 和 USD 两条
- 官方文档：[查询余额](https://api-docs.deepseek.com/zh-cn/api/get-user-balance/)

**推论 1**：官方接口只给「余额快照」，**不提供用量/花费流水**（故放弃花费估算）。

### 1.2 「实时」的本质：拉取 + 缓存

官方无推送机制，"实时"只能由拉取策略组合而成，且必须配缓存，避免频繁打官方接口：

| 拉取时机 | 本方案取值 |
|---|---|
| 后台定时轮询 | **30s**（`cordis-plugin-timer` 的 `ctx.setInterval`） |
| 会话轮次刷新（`agent/pre-step` 前） | 每轮取缓存注入（不再额外发请求） |
| 手动/工具查询 | `ds_balance` 工具 + **UI 手动刷新按钮**（强制穿透缓存立即查询） |

**推论 2**：30s TTL 缓存是硬约束——所有读方共享缓存；只有定时器到期和手动刷新才真正打官方接口；请求串行化（同一时刻最多一个在途请求）。

### 1.3 API key 从哪来？

dsh 本身已经持有 DeepSeek key——本机 `C:\Users\MsRain\.dsh\.credentials.yaml` 里存着 `DEEPSEEK_API_KEY`，由 `dsh-credentials` 服务管理（`dsh-llm-deepseek` 正是这样调用的，默认引用名就是 `DEEPSEEK_API_KEY`）。

插件通过凭证服务解析，零配置、永不落盘：

```ts
import { credentialRef } from '@deepseek-ai/dsh-credentials'

const ref = credentialRef(config.apiKeyEnv ?? 'DEEPSEEK_API_KEY')
const hit = await ctx.credentials.resolve(ref) // 每次操作时解析，凭证变更无需重启
```

**推论 3**：key 复用 dsh 现有凭证；插件不打印、不落盘、不缓存 key；`apiKeyEnv` 保留为配置项（多账号扩展口）。

### 1.4 展示载体：两条已选定的路径

| 载体 | 实现方式 | 成本 |
|---|---|---|
| 会话内工具 | 注册 `ds_balance` 工具，agent 随时可查 | 低（纯 host 侧） |
| 上下文注入 | `agent/pre-step` 每轮把余额行注入模型上下文（照抄 `dsh-time-context` 模式） | 低（纯 host 侧） |
| Web UI 常驻 | 前端 client 插件：侧边栏底部余额徽章 + 刷新按钮（slot + RPC） | 高（已验证机制，见 §3） |

### 1.5 硬约束

1. 余额请求**绝不能阻塞 agent 主循环**——失败静默降级（不注入、不报错打断对话）
2. key 永不进日志、不进 patch 配置、不跨插件暴露
3. 金额一律按**字符串**处理（官方就是字符串），禁浮点运算
4. 官方接口限流未知 → 30s TTL 缓存 + 请求串行化是硬要求

---

## 2. 架构设计

```
┌──────────────────────────── dsh host 进程（Node） ───────────────────────────┐
│  ┌─────────────┐   resolve(DEEPSEEK_API_KEY)   ┌───────────────────┐         │
│  │ credentials │ ─────────────────────────────▶│  BalanceService   │         │
│  │   服务      │                                │  （插件核心）       │         │
│  └─────────────┘                                │  fetch 官方接口     │         │
│  ┌─────────────┐   setInterval(30s)  ─────────▶│  TTL 缓存(30s)     │         │
│  │ timer 插件   │                                │  事件广播          │         │
│  └─────────────┘                                └──┬────┬─────┬─────┘         │
│                                                     │    │     │               │
│  ┌──────────────────────────────┐                   │    │     │               │
│  │ agent/pre-step 监听器          │◀── 每轮注入余额行    │    │     │               │
│  └──────────────────────────────┘                   │    │     │               │
│  ┌──────────────────────────────┐                   │    │     │               │
│  │ tools.register('ds_balance')  │◀── 会话内查询      │    │     │               │
│  └──────────────────────────────┘                   │    │     │               │
│  ┌──────────────────────────────┐                   │    │     │               │
│  │ 余额 remote RPC（api-remotes）│◀── 供浏览器调用     │    │     │               │
│  └──────────────┬───────────────┘                   │    │     │               │
└─────────────────┼───────────────────────────────────┴────┴─────┴───────────────┘
                  │ /api RPC（dsh-client-connection 的 HTTP/SSE 通道）
┌─────────────────┼─────────────────────── 浏览器（web profile） ────────────────┐
│  ┌──────────────▼───────────────┐                                               │
│  │ client 插件（React，client.js）│                                               │
│  │ · 注册 sidebar.footer.action   │◀── 侧边栏底部：余额徽章 + 手动刷新按钮          │
│  │ · 订阅 store 数据 / 调 remote   │                                               │
│  └──────────────────────────────┘                                               │
└────────────────────────────────────────────────────────────────────────────────┘
```

### 2.1 插件包形态：单包双面（`dsh.client` 双面模式）

参照 `@deepseek-ai/dsh-client-ui-goal`（已验证）的结构，一个 npm 包同时提供：

- **host 半**（`main` → `lib/index.js`）：BalanceService、轮询、注入、工具、remote RPC 注册
- **browser 半**（`exports["./client"]` → `lib/client.js`）：React 组件 + slot 注册
- `package.json` 声明 `"dsh": { "client": { "inject": [...], "platform": "web" } }` —— 被 `dsh-client-modules` 扫描进浏览器 roster，服务为 `/plugins/<id>/client.js`

### 2.2 插件主模块骨架（host 半，Cordis 4）

```ts
// src/index.ts —— host 半
import { Context } from '@deepseek-ai/cordis'
import z from '@deepseek-ai/schemastery'
import { credentialRef } from '@deepseek-ai/dsh-credentials'

export const name = 'balance-monitor'
export const inject = ['credentials']

export const Config = z.object({
  apiKeyEnv: z.string().default('DEEPSEEK_API_KEY'), // 凭证引用名（多账号扩展口）
  cacheTtlMs: z.number().default(30_000),            // 缓存 30s
  pollIntervalMs: z.number().default(30_000),        // 后台轮询 30s
  injectEveryTurn: z.boolean().default(true),        // 每轮注入余额
})

export function apply(ctx: Context, config: z.infer<typeof Config>) {
  // 1) BalanceService：凭证解析 → fetch 官方接口 → 缓存 → 事件广播
  // 2) ctx.setInterval 后台轮询
  // 3) agent/pre-step 注入监听器
  // 4) ctx.tools.register('ds_balance')
  // 5) remote RPC 注册（供浏览器调用，含手动刷新）
}
```

### 2.3 关键代码模式（均已在本机真实代码中验证）

**每轮注入**（同 `dsh-time-context` 的 `agent/pre-step` 模式）：

```ts
ctx.on('agent/pre-step', async ({ agent, turn, step }, next) => {
  const decision = await next()
  if (decision.kind === 'reject') return decision
  const balance = await balanceService.peek()   // 走缓存，不阻塞
  if (balance === undefined) return decision    // 查询失败：静默降级
  return { kind: 'enter', messages: [...decision.messages, /* 余额上下文消息 */] }
}, { prepend: true })
```

**官方接口调用**（host 进程内直连，无 pwsh 沙箱限制）：

```ts
const res = await fetch('https://api.deepseek.com/user/balance', {
  headers: { Authorization: `Bearer ${key}` },
  signal: AbortSignal.timeout(5_000),   // 5s 超时，绝不拖垮主循环
})
```

### 2.4 余额数据模型（字符串金额，禁浮点）

```ts
interface BalanceInfo {
  currency: 'CNY' | 'USD'
  totalBalance: string      // '110.00'
  grantedBalance: string
  toppedUpBalance: string
}
interface BalanceSnapshot {
  isAvailable: boolean
  infos: BalanceInfo[]
  fetchedAt: number        // 缓存时间戳
}
```

---

## 3. Web UI 技术路线（已侦察验证）

| 环节 | 机制（已核实） |
|---|---|
| 浏览器侧插件加载 | `package.json` 的 `dsh.client` 声明 + `./client` 导出；`dsh-client-modules` 扫描 roster、组装 `window.__DSH_BOOT__`、服务 `/plugins/<id>/client.js`；浏览器内由 `dsh-cordis-client-runner` 跑 cordis |
| UI 挂载点 | `@deepseek-ai/dsh-client-ui-slots` 的 `ctx.slots.register(options, component)`；侧边栏契约提供 **`sidebar.footer.action`**（`kind: 'list'`）—— Settings 旁的底部动作列表，正合适放余额徽章 + 刷新按钮 |
| 数据通道 | **修正（2026-08-15 侦察结论）**：Typert Remote（`@Remote` 装饰器 + `typert.host.js`）是 monorepo 构建链产物（`@deepseek-ai/dsh-typert-generator`），第三方插件不可行。改用官方 **session projection** 通道：host 半注册 projection face，client 半 `sessions.binding(id)?.session.projections.faceOf('balance')` 订阅（同 `dsh-goal` 的模式，零构建链）。刷新按钮的数据面继续侦察 `dsh-cordis-host-runner` 的动态 remote |
| 组件与依赖 | React 18（web profile 的 peerDependencies 版本）、`dsh-client-ui-primitives`、locale 机制 |
| 构建 | 官方包用 `tsdown` 把 client 半打成 `lib/client.js`；本地开发可用相同工具链 |
| 风险 | 官方 client bundle 由 `pnpm run dev:web` 重建；**第三方插件 client.js 的更新生效路径需在 M2 实测验证**（最坏情况：改 UI 后重启 `dsh web`） |

---

## 4. 项目结构

```
D:\Desktop\ds-balance-plugin\
├── 开发计划.md            # 本文档
├── package.json          # dsh.bundle + dsh.client 双声明
├── cordis.patch.yml      # 本包导出的 patch 层（host + client 两行）
├── tsconfig.json
├── src\
│   ├── index.ts          # host 半：服务/轮询/注入/工具/RPC
│   └── client.ts         # browser 半：React 徽章组件 + slot 注册
└── lib\                  # 构建产物（tsc + tsdown）
```

`package.json` 关键字段：

```json
{
  "name": "dsh-balance-monitor",
  "version": "0.1.0",
  "type": "module",
  "main": "lib/index.js",
  "exports": {
    ".": { "default": "./lib/index.js" },
    "./client": { "default": "./lib/client.js" }
  },
  "files": ["lib", "cordis.patch.yml"],
  "dsh": {
    "bundle": { "patch": "./cordis.patch.yml" },
    "client": {
      "inject": ["@deepseek-ai/dsh-client-runtime", "@deepseek-ai/dsh-api-remotes", "@deepseek-ai/dsh-client-ui-slots"],
      "platform": "web"
    }
  },
  "peerDependencies": {
    "@deepseek-ai/cordis": "^4.0.1",
    "@deepseek-ai/schemastery": "^3.18.1",
    "@deepseek-ai/dsh-credentials": "^0.1.0-rc.6",
    "@deepseek-ai/dsh-tools": "^0.1.0-rc.6",
    "@deepseek-ai/dsh-session": "^0.1.0-rc.6",
    "react": "^18.2.0"
  }
}
```

`cordis.patch.yml`：

```yaml
- insert:
    - id: balance-monitor
      name: dsh-balance-monitor
      config:
        apiKeyEnv: DEEPSEEK_API_KEY
        cacheTtlMs: 30000
        pollIntervalMs: 30000
        injectEveryTurn: true
```

安装与验证：

```sh
cd D:\Desktop\ds-balance-plugin
dsh plugin --profile web add .          # 装进 web profile（pnpm 转发，自动入 bundles 列表）
dsh --profile web --dump-config         # 检查组合后的配置树
dsh web                                 # 重启生效
```

---

## 5. 分阶段实施计划

### M1 —— host 侧核心：会话内可查可见 ✅ 已完成（2026-08-15）
- BalanceService（凭证解析 → fetch 官方接口 → 30s TTL 缓存 → 事件广播，请求串行化）
- 注册 `ds_balance` 工具（agent 会话里直接查询，返回币种/赠送/充值拆分）
- `agent/pre-step` 每轮注入余额行（开关 `injectEveryTurn`）
- `ctx.setInterval` 30s 后台轮询
- **验收记录（headless profile 实测）**：
  1. ✅ `dsh web` 配置树挂载正确（`--dump-config` 验证，schema 校验通过）
  2. ✅ 会话里 agent 调 `ds_balance` 返回真实余额（CNY 36.90，赠送 0 + 充值 36.90）
  3. ✅ `force=true` 穿透缓存正常
  4. ✅ 无 error/warning/`invalid output` 标记；失败降级文案正确（陈旧快照标注）
- **开发中踩坑（官方契约教训）**：
  1. `ctx.setInterval` 背后访问 `timer` 服务 → `inject` 必须声明 `'timer'`（冒烟测试抓到 "cannot get property timer without inject"）
  2. 工具 `execute` 返回值必须是**无损 JSON**——`undefined` 字段会触发 `invalid output: value is not lossless JSON`，可选值用 `0` 表示"无"
  3. schemastery 没有 `z.infer`，官方类型工具是 `Schemastery.TypeT<typeof Config>`（验证后类型）
  4. `createUserMessage` 是 `@deepseek-ai/dsh-llm` 的公开导出（注入上下文消息的标准构造器）

### M2 —— Web UI：侧边栏常驻余额 + 手动刷新按钮 ✅ 已完成（2026-08-15）
- **最终数据通道**：host 半 `TypertRemoteService`（服务键 `balance`，loader 行直接挂载 default 导出类）+ 手写 TYPERT strict 元数据（`./typert` 导出，typert-loader 注册，api-gateway 按 strict 定义认领 `/api/balance/*`）；browser 半用官方 RPC 公开协议直调（client-request 信封，POST `/api/balance/<method>`）——第三方 Typert Remote 客户端链路（`$mount` 贡献）实测静默失效，已弃用（详见《排障记录.md》第四段）
- browser 半：Matrix 徽章组件（磷光 CRT 风格，见《UI设计构思.md》）注册 `sidebar.footer.action` slot（wide/rail 双态）
- 30s 轮询 `balance/get`（走缓存）+ SYNC 按钮 `balance/refresh`（穿透刷新）+ 余额变化余辉闪烁
- 构建：tsc 双配置（host ESM + client CJS）→ `wrap-client.mjs` 纯字符串包壳成官方 `window.__ModuleLoader__.load` 注册格式 + `verify-client.mjs` 全链路自检
- **验收（浏览器 CDP 实测）**：徽章显示 `▸ BALANCE CNY ¥32.81 · LINK OK`，SYNC/30s 轮询正常，控制台无报错
- **回归修复记录**：
  1. 重构回归：服务类化时主插件模块丢失 `export const inject` 且类 `static inject` 缺 `tools` → headless 会话中 `ds_balance` 工具消失；诊断脚本（mock 服务 + 真实 TimerService）定位后修复
  2. 关键契约：`ctx.setInterval` 是 cordis-plugin-timer 通过 `ctx.mixin('timer', ...)` 混入 Context 原型的访问器——访问任何服务（含 mixin 面）都必须先声明 inject
  3. 构建链：esbuild 的 service 子进程被本机沙箱拦（spawn EPERM）→ 弃用，改 tsc 双配置 + 纯字符串包壳

### M3 —— 分发 ✅ 已完成（GitHub 直装；npm 发布留作可选）
- 发布前准备：package.json 元数据（license/repository/keywords/publishConfig）、LICENSE（MIT）、README 完善、文档中性化改名（`排障记录.md`）✅
- 分发方式：**GitHub 直装** `dsh plugin --profile web add github:Rainronin/dsh-balance-monitor`（dsh plugin 原生支持 git 托管）
- npm 发布：可选（作者暂无 npm 账号；package.json 的 publishConfig 已备好，有账号后 `npm publish` 即可）
- **验收**：仓库公开可达（https://github.com/Rainronin/dsh-balance-monitor）、话题 `dsh-plugin`、打包预览（npm pack --dry-run）10 文件 14.9 kB 内容正确

---

## 6. 风险与对策

| 风险 | 对策 |
|---|---|
| 官方接口字段变动 | 解析容错：缺字段→该币种跳过；整体失败→静默降级。以[官方文档](https://api-docs.deepseek.com/zh-cn/api/get-user-balance/)为准 |
| 接口限流 | 30s TTL 缓存硬约束 + 请求串行化（同时最多 1 个在途请求） |
| 金额精度 | 全程字符串透传展示，不做任何运算 |
| key 泄漏 | 仅从 credentials 服务解析；日志脱敏；patch 配置只含 ref 名 |
| 多币种 | 注入/徽章按 CNY 优先排序列出全部币种 |
| web 无 HMR | 改 host 半需重启 `dsh web`（本机端口 8808）；client 半的更新路径在 M2 实测，最坏情况同样重启 |
| 第三方 client.js 打包链路未知 | M2 开工先做 spike（最小徽章先上屏），失败则降级方案：**设置面板里的插件页**（`dsh-client-ui-settings-plugin-inventory` 已有插件清单 UI）展示余额入口 |
| 凭证未配置 | `resolve` 返回 undefined → 工具返回中文提示、注入跳过、轮询暂停、徽章显示未配置态 |
